import {describe, test, expect} from '@jest/globals';
import {Utils, utils} from '../../support/Utils'

describe('Utils Test Suite', () => {

    test('Exercise 1: Common matchers', () => {
        //Assert function utils.sum using common matchers
    })

    test('Exercise 2: String matchers', () => {
        //Assert function utils.createObjectWithDate using string matchers
    })

    test('Exercise 3: Truthiness matcher', () => {
        //Assert function utils.getLocalVar and utils.setLocalVar using truthiness matchers
        //utils.returnBoolean can be at use as well
    })

    test('Exercise 4: Numbers matcher', () => {
        //Assert function utils.divide using numbers matchers
    })

    test('Exercise 5: Arrays and Iterables matchers', () => {
        //Assert function utils.creteArray using Arrays and Iterables matchers
    })

    test('Exercise 5: Exceptions matchers', () => {
        //Assert function utils.throwException using Exceptions matchers
    })
})